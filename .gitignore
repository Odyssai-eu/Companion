node_modules
dist
.env
.env.local
.DS_Store
*.log
.vite

# Build artefact — regenerated by `tsc -b`. Tracking it makes every
# build dirty the worktree.
tsconfig.tsbuildinfo

# Internal session notes (local-only — not for public release).
session*.md

# Lockfiles from other package managers — the canonical one is
# package-lock.json (npm). Drop these if you've used pnpm/bun
# locally; we don't ship them.
pnpm-lock.yaml
bun.lock
yarn.lock

graphify-out/
